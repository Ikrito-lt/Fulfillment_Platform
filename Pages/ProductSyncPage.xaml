<Page x:Class="Ikrito_Fulfillment_Platform.Pages.ProductSyncPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Ikrito_Fulfillment_Platform.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductLoadPage">



    <Grid Margin="0"   Background="LightBlue">
        <StackPanel Orientation="Vertical" Margin="15,15,15,15">

            <Grid Background="Wheat">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Grid Margin="0">
                    <ProgressBar x:Name="loadingBar"  Minimum="0" Maximum="100" Height="25" Margin="10" VerticalAlignment="Bottom" Visibility="Visible" IsEnabled="true" />
                    <TextBlock Text="{Binding ElementName=loadingBar, Path=Value, StringFormat={}{0:0}%}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>

                <Button x:Name="UpdateProductsBtn" Click="UpdateProducts_Click" Content="Update Products" Padding="5" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5"/>
                <Button x:Name="BackBtn" Click="BackButton_Click" Content="Back" Padding="5" Grid.Column="0" Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5"/>
            </Grid>

        </StackPanel>
    </Grid>
</Page>
